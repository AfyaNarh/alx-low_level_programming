#include "main.h"

/**
*_isupper - Check if a letter is upper
*@c: The numberto be checked
*Return: 1 for upper letter or 0 for anything else
*/

int _isupper(int c)
{
	if (x >= 65 && <= 90)
	{
	return (1);
	}
	return (0);
}
